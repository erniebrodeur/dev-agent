interface:
  display_name: "Next Slice"
  short_description: "Implement one approved slice and stop"
  default_prompt: "Use $next-slice to implement exactly one approved implementation slice, verify it, update project memory, and stop."
policy:
  allow_implicit_invocation: true
