interface:
  display_name: "Copy Dev Agent Policy"
  short_description: "Install policy into a project AGENTS.md"
  default_prompt: "Use $copy-agents to propose installing Dev Agent policy in this project."
policy:
  allow_implicit_invocation: false
