interface:
  display_name: "Commit"
  short_description: "Prepare and approve a safe local commit"
  default_prompt: "Use $commit to prepare a local commit, show its staged scope and message, and wait for approval before committing."
policy:
  allow_implicit_invocation: true
