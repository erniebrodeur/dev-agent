interface:
  display_name: "Recover Project Context"
  short_description: "Reconstruct project state and next work"
  default_prompt: "Use $recover-project-context to determine where this project stands and what comes next."
policy:
  allow_implicit_invocation: true
