interface:
  display_name: "Commit"
  short_description: "Prepare and create an approved local commit"
  default_prompt: "Use $commit after a request to prepare, stage, or create a local commit, show its exact staged scope and message, and wait for approval before committing."
policy:
  allow_implicit_invocation: true
