interface:
  display_name: "Troubleshoot"
  short_description: "Diagnose failures and propose a correction"
  default_prompt: "Use $troubleshoot to diagnose this failure through direct evidence, propose a corrective slice, and wait for approval before editing implementation files."
policy:
  allow_implicit_invocation: true
