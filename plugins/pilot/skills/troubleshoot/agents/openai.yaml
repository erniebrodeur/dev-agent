interface:
  display_name: "Troubleshoot"
  short_description: "Diagnose failures and propose a correction"
  default_prompt: "Use $troubleshoot to diagnose this concrete failure when no correction is already approved, propose a corrective slice, and wait before implementation."
policy:
  allow_implicit_invocation: true
