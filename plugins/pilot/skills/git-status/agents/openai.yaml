interface:
  display_name: "Git Status"
  short_description: "Report Git state on request or before release"
  default_prompt: "Use $git-status to report the current repository state without changing it."
policy:
  allow_implicit_invocation: true
