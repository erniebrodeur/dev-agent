interface:
  display_name: "Next Slice"
  short_description: "Implement one approved slice and stop"
  default_prompt: "Use $next-slice to implement exactly one approved implementation slice, including an approved correction or utility slice, report whether its outcome is established, update project memory, and stop."
policy:
  allow_implicit_invocation: true
