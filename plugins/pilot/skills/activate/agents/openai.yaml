interface:
  display_name: "Activate Pilot"
  short_description: "Load policy and recover project context"
  default_prompt: "Use $activate to load Pilot policy, recover this project's current state, and report activation only after context recovery is complete."
policy:
  allow_implicit_invocation: true
