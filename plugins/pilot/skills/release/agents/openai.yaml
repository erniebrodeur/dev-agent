interface:
  display_name: "Release"
  short_description: "Create a distinct, verified version release"
  default_prompt: "Use $release to prepare and execute a version release with a dedicated version-bump commit and tag."
policy:
  allow_implicit_invocation: true
